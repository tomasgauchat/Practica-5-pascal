program ej8;
var minimo: integer;
    vueltas: integer;
    contador: integer;
    numero: integer;

begin
contador:= 2;
readln(vueltas);
  readln(numero);
  minimo:= numero;
  readln(numero);
  while (minimo < numero) and (contador < vueltas) do
    begin
      contador := contador + 1;
      readln(numero);
      if  then
    end;
  
end.